/**
 * gama-server-session.js — Unused (rendering handled by gama-app.js serverFetchAndRender)
 * Kept as reference for future server session features.
 */
var GamaServerSession = (function() {
    return {
        ENV_SIZE: 3000
    };
})();
